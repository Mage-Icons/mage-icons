{
  "name": "mage-icons-react",
  "version": "0.0.1",
  "description": "Finely constructed icons, perfect for deployment in web, iOS, Android, and desktop environments. Features support for SVG and web font technologies. Completely open-source, governed by the Apache License 2.0.",
  "main": "index.js",
  "scripts": {
    "build": "rm -rf dist && rm -rf index.js && rm -rf stroke && rm -rf bulk && NODE_ENV=production babel src/ --out-dir ./ --copy-files && rm -rf src"
  },
  "repository": "git@github.com:Mage-Icons/mage-icons-react.git",
  "author": "Mage Icons (https://mage-icons.web.app/)",
  "license": "Apache-2.0",
  "dependencies": {
    "react": "^18.2.0"
  },
  "private": true,
  "devDependencies": {
    "@babel/cli": "^7.22.5",
    "@babel/core": "^7.22.5",
    "@babel/preset-env": "^7.22.5",
    "@babel/polyfill": "^7.12.1",
    "@babel/preset-react": "^7.22.5"
  }
}
